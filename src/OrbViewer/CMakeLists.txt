fips_begin_app(OrbViewer windowed)
    fips_vs_warning_level(3)
    fips_files(Main.cc)
    #    oryol_shader(shaders.shd)
    fips_deps(Gfx HttpFS Input IMUI)
    # oryol_add_web_sample(MeshViewer "Load and render .orb files" "emscripten" none "OrbViewer/Main.cc")
fips_end_app()
